function []=Recommended_movie(BloomFilter)

Node=findMatches(tree,BloomFilter,root,Bank);
for i=4:D
    if tree(Node,i)==0
        j=i-1;
        break
    end %end of if
end % end of for
%??????????????????????

end % end of function